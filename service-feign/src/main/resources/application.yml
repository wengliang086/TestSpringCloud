server:
  port: 8765
spring:
  application:
    name: service-feign
eureka:
  client:
    service-url:
      defaultZone: http://admin:111111@localhost:8761/eureka/
feign:
  hystrix:
    enabled: true
